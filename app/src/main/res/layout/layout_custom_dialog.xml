<?xml version="1.0" encoding="utf-8"?>
<layout>
    <androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@drawable/dialog_background">

        <ImageView
            android:id="@+id/close"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="10dp"
            android:layout_marginEnd="10dp"
            android:src="@drawable/close_icon"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <TextView
            android:id="@+id/starredMessage"
            style="@style/TextAppearance.AppCompat.Text"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="20dp"
            android:layout_marginTop="20dp"
            android:layout_marginEnd="20dp"
            android:background="@drawable/dialog_text_background"
            android:gravity="center"
            android:text="@string/star_message"
            app:layout_constraintEnd_toEndOf="@id/close"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@id/close" />

        <TextView
            android:id="@+id/deleteMessage"
            style="@style/TextAppearance.AppCompat.Text"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginTop="20dp"
            android:background="@drawable/dialog_text_background"
            android:gravity="center"
            android:text="@string/delete_message"
            app:layout_constraintEnd_toEndOf="@id/starredMessage"
            app:layout_constraintStart_toStartOf="@id/starredMessage"
            app:layout_constraintTop_toBottomOf="@id/starredMessage" />
    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>